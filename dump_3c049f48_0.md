<USER_REQUEST>
Act as a Principal Staff Engineer specializing in enterprise Java systems. I need to scaffold the initial core microservices backend for "FinStream", a core banking and loan platform using Java 21, Maven, and Spring Boot 3.x. 

Generate the source code, build structures, and configuration for the first microservice: the `Account-Management-Service`.

Requirements to fulfill:
1. Provide a comprehensive Maven pom.xml utilizing Spring Boot 3.x and Java 21 properties. Include dependencies for Spring Web, Spring Data JPA, validation, and a relational database driver (PostgreSQL or Oracle XE).
2. Write the structural domain package containing an `Account` entity representing banking fields: id, accountHolderName, email, kycStatus (enum: PENDING, APPROVED, REJECTED), and balance (BigDecimal).
3. Create an explicit `AccountRepository` containing a hand-optimized native SQL query using `@Query` to fetch accounts containing balances greater than a specific threshold. Ensure proper positional parameters.
4. Build a RESTful Controller exposing standard CRUD operations including validation annotations (`@Valid`, `@NotNull`) to demonstrate high-quality request parsing.
5. Author an optimized production-grade, multi-stage Dockerfile for this Spring Boot application. The first stage must build the artifact using a Maven image (`maven:3.9-eclipse-temurin-21`), and the final runner stage must leverage a lightweight, secure JRE footprint (`eclipse-temurin:21-jre-jammy`). Include appropriate user permissions so it runs securely under non-root.
6. Provide a functional `docker-compose.yml` that stands up the application service alongside the relational database container, specifying health checks and correct environment variables.

Make the code exceptionally production-ready, highly scannable, and cleanly commented. Do not include empty templates or abstract text blocks.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-30T18:56:07+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.5 Flash (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>